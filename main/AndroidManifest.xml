<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.rupa.firebaserupa">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity android:name=".EditStory" />
        <activity android:name=".ViewStory" />
        <activity android:name=".ViewStory2" />
        <activity android:name=".ViewStoryInEditMode"></activity>
        <activity android:name=".OpeningScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity android:name=".HomePage">

        </activity>
        <receiver android:name=".BroadcastReceiver">
        <intent-filter>
            <action android:name="com.example.rupa.INTENT">

            </action>
        </intent-filter>
        </receiver>

    </application>

</manifest>
